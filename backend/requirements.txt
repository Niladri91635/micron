# ─── Milestone 1 (required now) ───────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pymupdf==1.24.9
python-docx==1.1.2
pydantic==2.8.2
pydantic-settings==2.4.0

# ─── Milestone 2 — LLM via xAI Grok (OpenAI-compatible) ─────────────────────
openai>=1.50.0          # used with xAI base_url — works for Grok, GPT-4o, etc.

# ─── Milestone 3 — PostgreSQL + pgvector (install when ready) ─────────────────
# asyncpg==0.29.0
# sqlalchemy[asyncio]==2.0.34
# alembic==1.13.2
# pgvector==0.3.2

# ─── Milestone 4 — Embeddings (install when ready) ───────────────────────────
# sentence-transformers==3.1.0
# numpy==1.26.4

# ─── Development ──────────────────────────────────────────────────────────────
# pytest==8.3.2
# httpx==0.27.2          # for TestClient in FastAPI tests
